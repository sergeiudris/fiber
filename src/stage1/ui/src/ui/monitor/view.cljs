(ns ui.monitor.view
  (:require  [reagent.core :as r]
             [re-frame.core :as rf]
             [cljs.repl :as repl]
             [cljs.pprint :as pp]
             [ui.monitor.events]
             [ui.monitor.subs]
             [ui.monitor.spec]
             [ui.monitor.core]
             #_["antd/lib/button" :default ant-Button]
             #_["antd/lib/table" :default AntTable]))


(comment

  ;
  )

(defn monitor-panel []
  (let []
    (fn []
      (let []
        [:div
         "monitor"
         ;
         ]
        ;
        ))))

(defn module-actions []
  [])
