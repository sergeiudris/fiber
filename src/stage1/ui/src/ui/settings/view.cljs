(ns ui.settings.view
  (:require  [reagent.core :as r]
             [re-frame.core :as rf]
             [cljs.repl :as repl]
             [cljs.pprint :as pp]
             [ui.settings.events]
             [ui.settings.subs]
             [ui.settings.spec]
             [fiber.spec :as rs]
             [ui.settings.core]
             #_["antd/lib/button" :default ant-Button]
             #_["antd/lib/table" :default AntTable]))


(comment

  ;
  )

(defn settings-panel []
  (let []
    (fn []
      (let []
        [:div
         "settings"
         ;
         ]
        ;
        ))))

(defn module-actions []
  [])
